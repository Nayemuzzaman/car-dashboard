package com.example.cardashboard.ui.theme

import androidx.compose.ui.unit.dp

/** Layout tokens, so sizes are named once instead of being repeated across the composables. */
object DashboardSpacing {
    val tiny = 4.dp
    val small = 8.dp
    val medium = 16.dp
    val screenPadding = 20.dp

    /** Minimum size for anything the driver taps. */
    val minTouchTarget = 48.dp

    val gaugeStroke = 14.dp
    val indicatorIcon = 26.dp
}
